# render.yaml

services:
  - name: web
    envVars:
      - key: FLASK_APP
        value: flaskMapperHelium.py
      - key: FLASK_ENV
        value: production
    startCommand: python flaskMapperHelium.py
    # Add other configurations as needed
